// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bettertime", "for bettertime support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bettertime", "enable bettertime support", "no");

if (PHP_BETTERTIME != "no") {
	EXTENSION("bettertime", "bettertime.c");
}

